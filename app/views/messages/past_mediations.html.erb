<div style="width: 100%; min-height: 100vh; padding: 20px 15px 100px 15px; box-sizing: border-box;">
  <div style="max-width: 1200px; margin: 0 auto;">
    <div class="tenant-header-card">
      <h1>Past Negotiations</h1>
      <p>View your completed and rejected negotiations below.</p>
      
      <div>
        <%= link_to "← Back to Messages", messages_path, class: "mediation-btn", style: "background: #6c757d; border-color: #6c757d;" %>
      </div>
    </div>

    <% if @past_mediations.any? %>
      <%= render 'tenants_old_mediations_table' %>
    <% else %>
      <div class="tenant-header-card" style="margin-top: 20px;">
        <p>You have no past negotiations.</p>
      </div>
    <% end %>
  </div>
</div>
